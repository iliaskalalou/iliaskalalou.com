export const BASE_URL = "https://iliaskalalou.com";

// Placeholder content is still live. Flip to true once the real experience,
// projects and links are written — that single change re-opens the site to
// search engines (robots.txt, sitemap and the page-level robots meta).
export const INDEXABLE = false;
